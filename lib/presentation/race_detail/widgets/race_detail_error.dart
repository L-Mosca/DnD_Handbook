import 'package:flutter/material.dart';

class RaceDetailError extends StatelessWidget {
  const RaceDetailError({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.red);
  }
}
