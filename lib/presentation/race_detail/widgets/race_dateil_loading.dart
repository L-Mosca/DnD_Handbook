import 'package:flutter/material.dart';

class RaceDetailLoading extends StatelessWidget {
  const RaceDetailLoading({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.blue);
  }
}
